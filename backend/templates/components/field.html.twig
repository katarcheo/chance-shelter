<div class="form-group">
    <label class="form-label" for="{{ id }}">{{ label }}</label>
    <input
        type="text"
        id="{{ id }}"
        name="{{ name }}"
        class="form-control"
        placeholder="{{ placeholder }}"
        value="{{ value ?? '' }}"
    >
</div>
